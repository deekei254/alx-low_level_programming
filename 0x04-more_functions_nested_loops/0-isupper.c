#include "main.h"
#include <stdio.h>

/**
 *_isupper - checks for uppercase character
 *@c: variable that prints uppercase inputs
 *Return: Always 0 if uppercase
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
return ());
}
